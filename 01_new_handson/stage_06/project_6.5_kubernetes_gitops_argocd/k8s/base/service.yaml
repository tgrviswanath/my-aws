apiVersion: v1
kind: Service
metadata:
  name: flask-api
  labels:
    app: flask-api
spec:
  selector:
    app: flask-api
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP   # internal only — Ingress handles external traffic
