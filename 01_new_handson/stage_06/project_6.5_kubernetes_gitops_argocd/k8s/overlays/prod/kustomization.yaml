apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

bases:
  - ../../base

patches:
  - path: patch-replicas.yaml

commonLabels:
  environment: prod

images:
  - name: ACCOUNT.dkr.ecr.us-east-1.amazonaws.com/handson-flask-api
    newTag: "1.0.0"   # prod: pinned to specific version
